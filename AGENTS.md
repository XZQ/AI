# Project Instructions

When starting work in this repository, read `docs/PROJECT_CONTEXT.md` first.

Use it as the primary project context before exploring other files.

This repository's Android entry module is `:app:car-store`.

Do not treat `:app:launcher` as the Android app entry point; it is a pure Java demo module.
